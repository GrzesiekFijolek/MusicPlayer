<UserControl x:Class="MusicPlayer.WPF.UserControls.SongInfoLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicPlayer.WPF.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border x:Name="SongInfoBorder"
            Style="{StaticResource OuterBorderForInnerShadow}">
        <Border Style="{StaticResource InternalBorderForInnerShadow}">
            <Label Style="{StaticResource SongInfo}" 
                   x:Name="InnerLabel"
                   Content="{Binding Path=(local:SongInfoLabel.LabelContent), ElementName=SongInfoBorder, Mode=TwoWay}"/>
        </Border>
    </Border>
</UserControl>
